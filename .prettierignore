build/
.svelte-kit/
dist/
node_modules/
coverage/
playwright-report/
test-results/
src/lib/components/ui/**
.vercel/
drizzle/
pnpm-lock.yaml
package-lock.json
yarn.lock
*.svg

# Review artifacts are generated reports + raw JSON dumps from review agents;
# don't reformat them on every CI run.
docs/reviews/

# Implementation plans + specs are agent-authored working docs with carefully
# hand-formatted code blocks / tables that the executing agents read verbatim;
# prettier reflow would churn them. Same rationale as docs/reviews/ above.
docs/plans/
docs/specs/

# Local Claude Code worktrees / scratch — not part of the repo.
.claude/

# Generated by scripts/build-splash.ts (pasted into src/app.html); not hand-formatted.
static/splash/_links.html
