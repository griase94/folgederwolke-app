// Phase 1's schema-author replaces this file with the full Drizzle schema.
// Do NOT add real table definitions here — this stub only exists so the
// schema barrel re-export compiles without errors during Phase 0.
export const __schemaPlaceholder = true;
