import Root, {
  type GateLineProps,
  type GateOption,
  type GateStatus,
} from "./GateLine.svelte";

export {
  Root,
  type GateLineProps as Props,
  //
  Root as GateLine,
  type GateLineProps,
  type GateOption,
  type GateStatus,
};
