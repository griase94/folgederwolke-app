node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Test artifacts
/playwright-report
/test-results
/coverage

# QA capture harness output (qa-capture.spec.ts / qa-live-capture.spec.ts /
# qa-dark-sweep.spec.ts)
.qa-shots/
.qa-shots-dark/
.qa-live/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
!.env.development

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local dev state (Postgres files go in a docker volume; this is for file storage + mail)
.dev-data/

# Claude tooling (worktrees, plans, agent state)
.claude/
# …but DO version the agent feedback memories so they survive across sessions
!.claude/memory/
!.claude/memory/**
.env*.local

# Visual brainstorming companion artifacts
.superpowers/
.build/
